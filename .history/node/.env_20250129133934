PORT=3000                      #Port for the server to run on
DB_HOST=localhost               #Database Host
DB_USER=root                    #Database User
DB_PASSWORD=@Rajulvish464       #Database Password
DB_NAME=oldSoulMarket           #Database Name
JWT_SECRET=@Rajulvish464        #JWT Secret
JWT_EXPIRES=7d                 #JWT Expiry Time


# /oldSoulMarket
# │
# ├── /node
# │   ├── /config
# │   │   └── db.js           # Database configuration
# │   │
# │   ├── /controllers
# │   │   └── userController.js # Controller for user logic (authentication, etc.)
# │   │
# │   ├── /models
# │   │   └── User.js         # Model for User
# │   │
# │   ├── /services
# │   │   └── authService.js  # Service for authentication logic
# │   │
# │   ├── /routes
# │   │   └── userRoutes.js   # Routes for user-related API endpoints
# │   │
# │   ├── /index.js           # Main entry point
# │   └── /.env               # Environment variables (JWT_SECRET, DB credentials, etc.)
# │
